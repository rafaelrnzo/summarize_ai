FROM python:3.11-slim

RUN apt-get update && apt-get install -y git build-essential ffmpeg && rm -rf /var/lib/apt/lists/*

RUN pip install --upgrade pip && \
    pip install whisper easyocr PyMuPDF unstructured